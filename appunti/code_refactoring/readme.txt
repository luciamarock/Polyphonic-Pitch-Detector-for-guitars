
	entrambe gli zip che stanno qui danno gli stessi risultati e sono i PitchDetectors per il pianoforte al punto in cui la logica e' ancora molto basilare 
	ricordo che nel PitchDetector per pianoforte avevo tolto la detection nel tempo che invece utilizzo nella chitarra (quindi poi bisognera' reintrodurla)
	la cosa importante di questi zip files e' che contengono il codice sviluppato in maniera migliore, solo che:
	piano_PitchDetector_luc.zip contiene le migliorie a livello di codice fatte da me (e che quindi non sono il top)
	inece piano_PolyphonicPitchDetection.zip contiene ulteriori migliorie fate da Gil che e' un C++ developer per cui il suo codice lo posso utilizzare per 
	studiare e imparare la prassi di programmazione in C++, in questo codice si vede anche l'utilizzo dei metodi di init() e l'utilizzo corretto delle classi
	la Tupla per esempio e' stata convertita in una classe vera e propria per cui occorre studiarne il funzionamento (e' una buona opportunita' per imparare)

	al momento (27/08) una copia di backup di piano_PitchDetector_luc.zip sta anche in /home/luciamarock/Documents/OnlinePianist/Bcks perche' ce la mette lo 
	script bck.sh pero' ovviamente questo puo' cambiare 

